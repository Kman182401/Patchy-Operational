---
name: push
description: Run the user's `push` shell alias (auto-commit and push Patchy_Bot to origin/main). Use when the user says `/push`, "run push", or asks to push the repo via the alias.
---

# Push

Runs the `push` shell alias, which is a zsh function that syncs skills, auto-commits any changes in `~/Patchy_Bot`, and pushes to `origin/main`.

## What to do

Run the alias in Bash — nothing else:

```
push
```

That's it. Do not add extra steps, checks, commits, or explanation. Report the command's output to the user.
